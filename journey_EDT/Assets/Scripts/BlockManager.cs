using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BlockManager : MonoBehaviour
{
   /*public GameObject[] blockPrefabs;
    public float xSpawn = 1.5F;
    public float blockLength = 40F;
    void Start()
    {
        SpawnBlock(0);
        SpawnBlock(1);

    }

    // Update is called once per frame
    void Update()
    {
        
    }
    public void SpawnBlock(int blockIndex){
        Instantiate(blockPrefabs[blockIndex],transform.forward * xSpawn,transform.rotation);
        xSpawn += blockLength;
    } */
}
